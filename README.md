# GhostShip — GitHub Pages

此 repository 僅存放 **正式 build 的靜態檔**，由 [`GhostShip`](https://github.com/lyshin617/GhostShip) 的 GitHub Actions 自動部署。

- 線上遊玩：https://lyshin617.github.io/GhostShip-pages/
- 請勿手動修改 `index.html` 與 `assets/`；變更請於主 repo 的 `web/` 開發後 merge 至 `main`。
